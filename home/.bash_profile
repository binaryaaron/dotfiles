#!/usr/bin/env bash
# ~/.bash_profile: executed by bash(1) for login shells.
# Source .bashrc so login and non-login shells get identical config.

[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"

. "$HOME/.atuin/bin/env"
