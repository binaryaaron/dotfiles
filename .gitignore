# Runtime state and secrets (safety net for any tool under config/)
**/*.db
**/*.db-shm
**/*.db-wal
**/key

# mise per-machine configuration and secrets
**/mise.local.toml
**/.mise.local.toml

# atuin install receipt
config/atuin/atuin-receipt.json

# Cursor local state and secrets
config/cursor/auth.json
config/cursor/cli-config.json
config/cursor/statsig-cache.json

# macOS junk
.DS_Store
