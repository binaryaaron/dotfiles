# shellcheck shell=bash
#
# ble.sh reads this file from ${XDG_CONFIG_HOME:-$HOME/.config}/blesh/init.sh.
# Keep the initial defaults quiet; opt into visible editor features gradually.

bleopt exec_elapsed_mark=
bleopt exec_errexit_mark=
bleopt exec_exit_mark=
